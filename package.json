{
  "name": "emergencywallet",
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run deploy.js --network monad"
  },
  "devDependencies": {
    "hardhat": "^2.16.3",
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "ethers": "^5.7.2",
    "dotenv": "^17.2.1"
  }
}
